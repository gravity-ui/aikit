import {composeStories} from '@storybook/react';

import * as DefaultToolFooterStories from '../__stories__/ToolFooter.stories';

export const ToolFooterStories = composeStories(DefaultToolFooterStories);
