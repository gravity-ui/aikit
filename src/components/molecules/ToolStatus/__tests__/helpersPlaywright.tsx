import {composeStories} from '@storybook/react';

import * as DefaultToolStatusStories from '../__stories__/ToolStatus.stories';

export const ToolStatusStories = composeStories(DefaultToolStatusStories);
