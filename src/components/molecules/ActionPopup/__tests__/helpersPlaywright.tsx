import {composeStories} from '@storybook/react';

import * as DefaultActionPopupStories from '../__stories__/ActionPopup.stories';

export const ActionPopupStories = composeStories(DefaultActionPopupStories);
