import {composeStories} from '@storybook/react';

import * as DefaultRatingBlockStories from '../__stories__/RatingBlock.stories';

export const RatingBlockStories = composeStories(DefaultRatingBlockStories);
