import {composeStories} from '@storybook/react';

import * as DefaultInputContextStories from '../__stories__/InputContext.stories';

export const InputContextStories = composeStories(DefaultInputContextStories);
