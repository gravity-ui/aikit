import {composeStories} from '@storybook/react';

import * as DefaultContextItemStories from '../__stories__/ContextItem.stories';

export const ContextItemStories = composeStories(DefaultContextItemStories);
