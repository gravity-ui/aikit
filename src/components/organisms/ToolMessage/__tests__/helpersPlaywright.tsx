import {composeStories} from '@storybook/react';

import * as DefaultToolMessageStories from '../__stories__/ToolMessage.stories';

export const ToolMessageStories = composeStories(DefaultToolMessageStories);
