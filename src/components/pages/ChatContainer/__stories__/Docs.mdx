import {Meta, Canvas, Controls, Markdown} from '@storybook/addon-docs';
import * as Stories from './ChatContainer.stories';
import Readme from '../README.md?raw';

<Meta of={Stories} />

For flat copy overrides, use the **`texts`** prop (`ChatContainerTexts`); see the **Texts** section in the README below.

<Markdown>{Readme}</Markdown>

## Playground

<Canvas of={Stories.Playground} />
<Controls of={Stories.Playground} />
